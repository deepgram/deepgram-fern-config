---
title: LLM Models
subtitle: >-
  An overview of the LLM providers and models you can use with the Voice Agent
  API.
slug: docs/voice-agent-llm-models
---


[Voice Agent](/docs/voice-agent)

Defines the LLM (*Large Language Model*) to be used with your Agent.

<Info>
  You can set your LLM model in the [Setting Configuration](/docs/voice-agent-settings-configuration) for your Voice Agent. See the docs for more information.
</Info>

## Supported LLM providers and models

| think.provider.type | think.model values        | think.provider.url values  | think.provider.headers values                   |
| ------------------- | ------------------------- | -------------------------- | ----------------------------------------------- |
| `open_ai`           | `gpt-4o-mini`             | none                       | none                                            |
| `anthropic`         | `claude-3-haiku-20240307` | none                       | none                                            |
| `custom`            | `YOUR_MODEL_NAME`         | `YOUR_LLM_COMPLETIONS_URL` | `Array of Headers to pass with your auth token` |

## Example

<CodeGroup>
  ```json json
  {
    "think": {
      "provider": {
        "type": "open_ai"
      },
      "instructions": "this is the LLM System prompt",
      "model": "gpt-4o-mini"
    }
  }
  ```
</CodeGroup>

## Passing a custom LLM through a Cloud Provider

You can pass a custom chat completions URL and headers to set your authorization or api-key etc

<CodeGroup>
  ```json json
  {
    "think": {
      "provider": {
        "type": "custom",
        "url": "YOUR_LLM_CHAT_COMPLETIONS_URL",
        "headers": [
          {
            "key": "Authorization",
            "value": "Bearer sk-xxx"
          }
        ]
      },
      "instructions": "this is the LLM System prompt",
      "model": "your-model-name"
    }
  }
  ```
</CodeGroup>

### Azure OpenAI Service

* Checkout the docs on[ how to create and deploy an Azure OpenAI](https://learn.microsoft.com/en-us/azure/ai-services/openai/how-to/create-resource?pivots=web-portal) service resource.
* See this [code repository](https://github.com/deepgram-devs/voice-agent-azure-open-ai-services) for a getting started example.

### Amazon Bedrock

* [Checkout our workshop](https://catalog.us-east-1.prod.workshops.aws/workshops/318f009b-6147-46b2-a86c-0e4a52866016/en-US) on using the Deepgram Voice Agent API with Amazon Bedrock.
* See this [code repository](https://github.com/DamienDeepgram/amazon-bedrock-deepgram-voice-agent-client) for a getting started example.
